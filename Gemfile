source "https://rubygems.org"

ruby ">= 3.3"

# The sync itself is stdlib only. RSpec is here so CI can run the suite
# before touching the live store.
group :development, :test do
  gem "rspec", "~> 3.13"
end
